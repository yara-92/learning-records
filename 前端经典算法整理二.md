# 前端经典算法整理（二）

承接上一份文档，继续补充 12 个高频算法，编号从 13 开始，侧重手写核心 API、数据结构操作、以及实际业务场景（虚拟列表、懒加载、树形数据）。

---

## 13. 手写 new 操作符

**场景**：理解构造函数与原型链的关系，是 JS 底层原理必考题。

```javascript
function myNew(Ctor, ...args) {
  if (typeof Ctor !== 'function') {
    throw new TypeError('myNew 的第一个参数必须是函数');
  }
  // 1. 创建新对象，原型指向构造函数的 prototype
  const obj = Object.create(Ctor.prototype);
  // 2. 执行构造函数，this 指向新对象
  const result = Ctor.apply(obj, args);
  // 3. 如果构造函数返回的是对象，则返回该对象；否则返回新创建的 obj
  return (typeof result === 'object' && result !== null) ? result : obj;
}

function Person(name) { this.name = name; }
Person.prototype.sayHi = function () { console.log(`Hi, ${this.name}`); };

const p = myNew(Person, '小明');
p.sayHi(); // Hi, 小明
```

**要点**：核心四步——创建对象、绑定原型、绑定 this 执行构造函数、判断返回值类型决定最终返回谁。

---

## 14. 手写 instanceof

**场景**：理解原型链查找机制。

```javascript
function myInstanceof(instance, Ctor) {
  if (typeof Ctor !== 'function') throw new TypeError('右侧必须是函数');
  let proto = Object.getPrototypeOf(instance);
  const prototype = Ctor.prototype;

  while (proto !== null) {
    if (proto === prototype) return true;
    proto = Object.getPrototypeOf(proto);
  }
  return false;
}

console.log(myInstanceof([], Array));  // true
console.log(myInstanceof([], Object)); // true（原型链向上能找到 Object.prototype）
```

**要点**：沿着实例的 `__proto__` 链一直向上找，直到等于构造函数的 `prototype` 或到达 `null`。

---

## 15. 手写 Promise.all / Promise.race / Promise.allSettled

**场景**：并发请求控制，是异步编程手写题中的进阶版本。

```javascript
// Promise.all：全部成功才成功，任一失败立即失败
function myPromiseAll(promises) {
  return new Promise((resolve, reject) => {
    const results = [];
    let completed = 0;
    if (promises.length === 0) return resolve([]);

    promises.forEach((p, index) => {
      Promise.resolve(p).then((value) => {
        results[index] = value; // 保证顺序与传入一致
        completed++;
        if (completed === promises.length) resolve(results);
      }).catch(reject);
    });
  });
}

// Promise.race：谁先完成（无论成功失败）就用谁的结果
function myPromiseRace(promises) {
  return new Promise((resolve, reject) => {
    promises.forEach((p) => {
      Promise.resolve(p).then(resolve).catch(reject);
    });
  });
}

// Promise.allSettled：等待全部完成，返回每个的状态和结果
function myPromiseAllSettled(promises) {
  return Promise.all(
    promises.map((p) =>
      Promise.resolve(p)
        .then((value) => ({ status: 'fulfilled', value }))
        .catch((reason) => ({ status: 'rejected', reason }))
    )
  );
}
```

**要点**：`all` 要用 `index` 记录顺序（因为异步完成顺序不确定但结果顺序要保持一致）；`allSettled` 巧妙地把每个 promise 转成"一定 resolve"的形式再用 `all` 组合。

---

## 16. 链表反转

**场景**：虽然前端较少直接操作链表，但面试常考，且能迁移到 React Fiber 树（本质是链表结构）的理解上。

```javascript
function createLinkedList(arr) {
  let head = null, tail = null;
  for (const val of arr) {
    const node = { val, next: null };
    if (!head) { head = node; tail = node; }
    else { tail.next = node; tail = node; }
  }
  return head;
}

// 迭代法反转
function reverseList(head) {
  let prev = null, curr = head;
  while (curr) {
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev; // 新的头节点
}

function printList(head) {
  const res = [];
  while (head) { res.push(head.val); head = head.next; }
  return res.join(' -> ');
}

const list = createLinkedList([1, 2, 3, 4, 5]);
console.log(printList(reverseList(list))); // 5 -> 4 -> 3 -> 2 -> 1
```

**要点**：用三个指针 `prev/curr/next` 依次改变每个节点的指向，时间复杂度 O(n)，空间复杂度 O(1)。

---

## 17. 树的遍历（DFS / BFS）

**场景**：DOM 树遍历、组件树查找节点、菜单树展开/搜索。

```javascript
const tree = {
  val: 1,
  children: [
    { val: 2, children: [{ val: 4, children: [] }] },
    { val: 3, children: [{ val: 5, children: [] }, { val: 6, children: [] }] },
  ],
};

// 深度优先（递归）—— 常用于查找、路径收集
function dfs(node, path = []) {
  if (!node) return;
  path.push(node.val);
  node.children.forEach((child) => dfs(child, path));
  return path;
}

// 广度优先（队列）—— 常用于按层处理、最短路径
function bfs(root) {
  const result = [];
  const queue = [root];
  while (queue.length) {
    const node = queue.shift();
    result.push(node.val);
    queue.push(...node.children);
  }
  return result;
}

console.log(dfs(tree)); // [1, 2, 4, 3, 5, 6]
console.log(bfs(tree)); // [1, 2, 3, 4, 5, 6]
```

**要点**：DFS 用递归（或显式栈）"一条路走到黑"，BFS 用队列"一层一层"扩散；`document.querySelectorAll` 内部遍历、React Fiber 的遍历都基于类似思想。

---

## 18. 扁平数组转树形结构（List to Tree）

**场景**：后端返回的部门列表、菜单列表（带 `id` / `parentId`）转换为前端所需的树形组件数据。

```javascript
function listToTree(list, rootId = 0) {
  const map = new Map();
  list.forEach((item) => map.set(item.id, { ...item, children: [] }));

  const tree = [];
  map.forEach((item) => {
    if (item.parentId === rootId) {
      tree.push(item);
    } else {
      const parent = map.get(item.parentId);
      if (parent) parent.children.push(item);
    }
  });
  return tree;
}

const list = [
  { id: 1, parentId: 0, name: '部门A' },
  { id: 2, parentId: 1, name: '部门A-1' },
  { id: 3, parentId: 1, name: '部门A-2' },
  { id: 4, parentId: 0, name: '部门B' },
];
console.log(JSON.stringify(listToTree(list), null, 2));
```

**要点**：先用 `Map` 建立 `id → 节点` 的索引（O(1) 查找），再遍历一次挂载到对应父节点，整体时间复杂度 O(n)，避免了嵌套循环的 O(n²)。

---

## 19. Fisher-Yates 洗牌算法

**场景**：抽奖随机化、随机题目顺序、随机播放列表。相比 `sort(() => Math.random() - 0.5)` 这种伪随机写法，能保证真正均匀分布。

```javascript
function shuffle(arr) {
  const result = [...arr];
  for (let i = result.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1)); // 从 [0, i] 中随机选一个
    [result[i], result[j]] = [result[j], result[i]];
  }
  return result;
}

console.log(shuffle([1, 2, 3, 4, 5]));
```

**要点**：从后往前遍历，每次从"未处理部分"中随机选一个元素交换到当前位置，数学上可证明每种排列出现概率相等；`sort` 随机比较函数的方式在 V8 等引擎中并不保证均匀分布。

---

## 20. 千分位/金额格式化

**场景**：订单金额展示、数据大屏数字展示。

```javascript
// 正则法（简洁，支持负数和小数）
function formatMoney(num) {
  const [intPart, decimalPart] = String(num).split('.');
  const formattedInt = intPart.replace(/\B(?=(\d{3})+(?!\d))/g, ',');
  return decimalPart ? `${formattedInt}.${decimalPart}` : formattedInt;
}

console.log(formatMoney(1234567.89)); // 1,234,567.89
console.log(formatMoney(-98765));     // -98,765

// 更推荐：原生 Intl API（自动处理国际化、货币符号）
function formatCurrency(num, locale = 'zh-CN', currency = 'CNY') {
  return new Intl.NumberFormat(locale, { style: 'currency', currency }).format(num);
}
console.log(formatCurrency(1234567.89)); // ¥1,234,567.89
```

**要点**：正则 `\B(?=(\d{3})+(?!\d))` 利用零宽断言在每三位数字前插入逗号；生产环境优先使用浏览器原生 `Intl.NumberFormat`，能自动处理千分位、货币符号、多语言。

---

## 21. 数组交集 / 并集 / 差集

**场景**：权限对比、标签筛选、多条件数据过滤。

```javascript
const a = [1, 2, 3, 4], b = [3, 4, 5, 6];

const intersection = (a, b) => {
  const setB = new Set(b);
  return [...new Set(a)].filter((x) => setB.has(x));
};
const union = (a, b) => [...new Set([...a, ...b])];
const difference = (a, b) => {
  const setB = new Set(b);
  return a.filter((x) => !setB.has(x));
};

console.log(intersection(a, b)); // [3, 4]
console.log(union(a, b));        // [1, 2, 3, 4, 5, 6]
console.log(difference(a, b));   // [1, 2]
```

**要点**：全部基于 `Set` 的 O(1) 查找特性，将朴素的 O(n²) 双重遍历优化到 O(n)。

---

## 22. URL 查询参数解析与序列化

**场景**：不依赖第三方库时手动处理 `location.search`，或构造请求 URL。

```javascript
// 解析：'?a=1&b=2&c=3' -> { a: '1', b: '2', c: '3' }
function parseQuery(url) {
  const query = {};
  const search = url.split('?')[1];
  if (!search) return query;

  search.split('&').forEach((pair) => {
    const [key, value = ''] = pair.split('=');
    query[decodeURIComponent(key)] = decodeURIComponent(value);
  });
  return query;
}

// 序列化：{ a: 1, b: 2 } -> 'a=1&b=2'
function stringifyQuery(obj) {
  return Object.keys(obj)
    .map((key) => `${encodeURIComponent(key)}=${encodeURIComponent(obj[key])}`)
    .join('&');
}

console.log(parseQuery('https://x.com?a=1&b=hello%20world')); // { a: '1', b: 'hello world' }
console.log(stringifyQuery({ a: 1, b: 'hello world' }));       // a=1&b=hello%20world
```

**要点**：生产环境优先使用原生 `URLSearchParams`（`new URLSearchParams(location.search)`），手写版本用于理解原理或兼容极老环境。

---

## 23. 虚拟列表核心算法（可视区域计算）

**场景**：长列表性能优化（聊天记录、表格万行数据），只渲染可视区域内的 DOM，大幅减少节点数量。

```javascript
function getVisibleRange({ scrollTop, itemHeight, containerHeight, totalCount, buffer = 3 }) {
  // 当前视口第一个可见项的索引
  const startIndex = Math.max(0, Math.floor(scrollTop / itemHeight) - buffer);
  // 视口内能容纳的项数（向上取整保证边缘不留白）
  const visibleCount = Math.ceil(containerHeight / itemHeight);
  const endIndex = Math.min(totalCount - 1, startIndex + visibleCount + buffer * 2);

  return {
    startIndex,
    endIndex,
    offsetY: startIndex * itemHeight, // 用于 transform: translateY 定位
    totalHeight: totalCount * itemHeight, // 撑开滚动条高度的占位容器高度
  };
}

// 示例：10000 条数据，每项 50px，容器高度 600px，当前滚动了 2000px
console.log(getVisibleRange({
  scrollTop: 2000, itemHeight: 50, containerHeight: 600, totalCount: 10000,
}));
// { startIndex: 37, endIndex: 55, offsetY: 1850, totalHeight: 500000 }
```

**要点**：核心思路是用一个"总高度占位元素"撑开滚动条，再用 `translateY` 把真实渲染的少量 DOM 定位到正确视觉位置；`buffer` 预渲染上下若干项，避免快速滚动时出现白屏闪烁。

---

## 24. 图片懒加载（IntersectionObserver 实现）

**场景**：长页面图片列表首屏性能优化，只在图片进入视口附近时才加载真实图片。

```javascript
function lazyLoadImages(selector = 'img[data-src]', rootMargin = '100px') {
  const images = document.querySelectorAll(selector);

  const observer = new IntersectionObserver((entries, obs) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const img = entry.target;
        img.src = img.dataset.src; // 触发真实图片加载
        img.removeAttribute('data-src');
        obs.unobserve(img); // 加载后立即取消观察，节省性能
      }
    });
  }, { rootMargin }); // 提前 100px 开始加载，避免用户看到空白

  images.forEach((img) => observer.observe(img));
  return observer;
}

// 使用：<img data-src="real.jpg" src="placeholder.jpg" />
// lazyLoadImages();
```

**要点**：`IntersectionObserver` 是浏览器原生 API，相比早期监听 `scroll` 事件手动计算 `getBoundingClientRect()` 性能好得多（不阻塞主线程、不用节流）；`rootMargin` 让图片提前一段距离开始加载，提升用户体验。

---

## 复杂度速查表（续）

| 算法 | 时间复杂度 | 空间复杂度 |
|---|---|---|
| 手写 new / instanceof | O(1) / O(原型链深度) | O(1) |
| Promise.all 系列 | O(n) | O(n) |
| 链表反转 | O(n) | O(1) |
| DFS / BFS | O(n) | O(n) |
| List to Tree | O(n) | O(n) |
| Fisher-Yates 洗牌 | O(n) | O(n) |
| 千分位格式化 | O(n) | O(n) |
| 交集/并集/差集 | O(n) | O(n) |
| 虚拟列表区间计算 | O(1) | O(1) |
| IntersectionObserver 懒加载 | O(1)（浏览器原生优化） | O(1) |

---

至此两份文档共 24 个前端经典算法，覆盖了「性能优化」「数据结构」「异步编程」「手写核心 API」「实际业务场景」五大类，基本囊括了前端面试和日常开发中最常遇到的算法题型。
