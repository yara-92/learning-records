# 前端经典算法整理（JavaScript 实现）

涵盖高频手写题、性能优化算法、数据结构操作，共 12 个专题。每个专题包含：**应用场景 → 思路分析 → 代码实现 → 复杂度**。

---

## 1. 防抖（Debounce）

**场景**：搜索框输入联想、窗口 resize、按钮防止重复提交。事件触发后等待一段时间，若期间再次触发则重新计时，只在"停止触发"后执行一次。

```javascript
function debounce(fn, delay = 300, immediate = false) {
  let timer = null;
  return function (...args) {
    const context = this;
    if (timer) clearTimeout(timer);

    if (immediate && !timer) {
      fn.apply(context, args);
    }

    timer = setTimeout(() => {
      timer = null;
      if (!immediate) fn.apply(context, args);
    }, delay);
  };
}

// 使用示例
const onInput = debounce((e) => console.log('搜索:', e.target.value), 500);
// input.addEventListener('input', onInput);
```

**要点**：`immediate` 参数控制是否首次触发立即执行（用于按钮防重复点击场景）。

---

## 2. 节流（Throttle）

**场景**：滚动加载、鼠标 mousemove、拖拽。保证单位时间内函数最多执行一次，与防抖不同的是它会"定期执行"而非"只执行最后一次"。

```javascript
// 时间戳版：首次立即触发
function throttle(fn, interval = 300) {
  let last = 0;
  return function (...args) {
    const now = Date.now();
    if (now - last >= interval) {
      last = now;
      fn.apply(this, args);
    }
  };
}

// 定时器版：首次延迟触发，保证最后一次也会执行
function throttleTimer(fn, interval = 300) {
  let timer = null;
  return function (...args) {
    if (timer) return;
    timer = setTimeout(() => {
      fn.apply(this, args);
      timer = null;
    }, interval);
  };
}
```

**要点**：两种实现的差异在于"首触发是否立即执行"，工程中常结合使用（首尾都触发）。

---

## 3. 深拷贝（Deep Clone）

**场景**：Redux/Vuex 状态复制、表单数据备份。需处理循环引用、Date、RegExp、Map、Set 等特殊类型。

```javascript
function deepClone(target, map = new WeakMap()) {
  if (target === null || typeof target !== 'object') return target;
  if (target instanceof Date) return new Date(target);
  if (target instanceof RegExp) return new RegExp(target);
  if (map.has(target)) return map.get(target); // 处理循环引用

  const cloneTarget = Array.isArray(target) ? [] : {};
  map.set(target, cloneTarget);

  if (target instanceof Map) {
    target.forEach((v, k) => cloneTarget.set(k, deepClone(v, map)));
  } else if (target instanceof Set) {
    target.forEach((v) => cloneTarget.add(deepClone(v, map)));
  } else {
    Reflect.ownKeys(target).forEach((key) => {
      cloneTarget[key] = deepClone(target[key], map);
    });
  }
  return cloneTarget;
}
```

**要点**：用 `WeakMap` 记录已拷贝对象解决循环引用问题；`Reflect.ownKeys` 能拿到 Symbol 类型的 key。

---

## 4. 数组扁平化（Flatten）

**场景**：处理树形结构转平铺列表、多维数据规整。

```javascript
// 方法一：递归
function flatten(arr) {
  return arr.reduce((res, item) => {
    return res.concat(Array.isArray(item) ? flatten(item) : item);
  }, []);
}

// 方法二：栈迭代（避免递归爆栈）
function flattenIterative(arr) {
  const stack = [...arr];
  const res = [];
  while (stack.length) {
    const next = stack.pop();
    if (Array.isArray(next)) {
      stack.push(...next);
    } else {
      res.push(next);
    }
  }
  return res.reverse();
}

console.log(flatten([1, [2, [3, [4, 5]]], 6])); // [1,2,3,4,5,6]
```

**要点**：递归版简洁但深层嵌套可能栈溢出，栈迭代版更安全，是大厂手写题常考点。

---

## 5. 数组去重（Unique）

**场景**：处理接口返回的重复数据、标签去重。

```javascript
// ES6 Set（仅适用于基本类型）
const unique1 = (arr) => [...new Set(arr)];

// 支持 NaN、对象引用去重的通用版本
function uniqueDeep(arr) {
  const map = new Map();
  return arr.filter((item) => {
    const key = typeof item === 'object' ? JSON.stringify(item) : item;
    if (map.has(key)) return false;
    map.set(key, true);
    return true;
  });
}
```

**要点**：`Set` 底层用 `SameValueZero` 算法比较，能正确处理 `NaN`（`indexOf` 做不到）。

---

## 6. 函数柯里化（Curry）

**场景**：函数式编程、参数复用（如 Redux 的 `connect(mapState)(Component)`）。

```javascript
function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) {
      return fn.apply(this, args);
    }
    return (...args2) => curried.apply(this, args.concat(args2));
  };
}

function add(a, b, c) { return a + b + c; }
const curriedAdd = curry(add);
console.log(curriedAdd(1)(2)(3));    // 6
console.log(curriedAdd(1, 2)(3));    // 6
console.log(curriedAdd(1, 2, 3));    // 6
```

**要点**：核心是利用 `fn.length` 判断已收集参数是否够用，不够则返回新函数继续收集（闭包保存已传参数）。

---

## 7. 二分查找（Binary Search）

**场景**：有序数组中快速定位、虚拟列表滚动位置计算。时间复杂度 O(log n)，远快于线性查找的 O(n)。

```javascript
function binarySearch(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left <= right) {
    const mid = left + ((right - left) >> 1); // 防止溢出
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1; // 未找到
}

console.log(binarySearch([1, 3, 5, 7, 9, 11], 7)); // 3
```

**要点**：数组必须有序；`>> 1` 位运算求中点比 `Math.floor((left+right)/2)` 更快且避免相加溢出。

---

## 8. 快速排序（Quick Sort）

**场景**：前端排序需求（表格排序、数据展示），面试高频算法题，平均时间复杂度 O(n log n)。

```javascript
function quickSort(arr) {
  if (arr.length <= 1) return arr;
  const pivot = arr[Math.floor(arr.length / 2)];
  const left = [], right = [], equal = [];

  for (const num of arr) {
    if (num < pivot) left.push(num);
    else if (num > pivot) right.push(num);
    else equal.push(num);
  }
  return [...quickSort(left), ...equal, ...quickSort(right)];
}

console.log(quickSort([5, 2, 9, 1, 5, 6])); // [1,2,5,5,6,9]
```

**要点**：这是易读的"三路划分"写法；原地交换版更省内存但更复杂，面试中说明思路即可。

---

## 9. 简易 Promise 实现（手写 Promise/A+ 核心）

**场景**：理解异步编程原理，是资深前端面试必考题。

```javascript
class MyPromise {
  static PENDING = 'pending';
  static FULFILLED = 'fulfilled';
  static REJECTED = 'rejected';

  constructor(executor) {
    this.status = MyPromise.PENDING;
    this.value = undefined;
    this.callbacks = []; // 存储 pending 状态下的回调

    const resolve = (value) => {
      if (this.status !== MyPromise.PENDING) return;
      this.status = MyPromise.FULFILLED;
      this.value = value;
      this.callbacks.forEach((cb) => cb.onFulfilled(value));
    };
    const reject = (reason) => {
      if (this.status !== MyPromise.PENDING) return;
      this.status = MyPromise.REJECTED;
      this.value = reason;
      this.callbacks.forEach((cb) => cb.onRejected(reason));
    };

    try {
      executor(resolve, reject);
    } catch (e) {
      reject(e);
    }
  }

  then(onFulfilled, onRejected) {
    return new MyPromise((resolve, reject) => {
      const handleFulfilled = (value) => {
        try {
          resolve(typeof onFulfilled === 'function' ? onFulfilled(value) : value);
        } catch (e) { reject(e); }
      };
      const handleRejected = (reason) => {
        try {
          if (typeof onRejected === 'function') resolve(onRejected(reason));
          else reject(reason);
        } catch (e) { reject(e); }
      };

      if (this.status === MyPromise.FULFILLED) {
        queueMicrotask(() => handleFulfilled(this.value));
      } else if (this.status === MyPromise.REJECTED) {
        queueMicrotask(() => handleRejected(this.value));
      } else {
        this.callbacks.push({ onFulfilled: handleFulfilled, onRejected: handleRejected });
      }
    });
  }
}
```

**要点**：核心是状态机（三态不可逆）+ 发布订阅（pending 时缓存回调）+ 微任务模拟异步。真实实现还需处理链式调用中返回值是 Promise 的情况（递归解析）。

---

## 10. LRU 缓存（Least Recently Used）

**场景**：浏览器缓存策略、前端数据缓存（如图片缓存池、Vue keep-alive 原理）。

```javascript
class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.cache = new Map(); // Map 天然保持插入顺序
  }

  get(key) {
    if (!this.cache.has(key)) return -1;
    const value = this.cache.get(key);
    this.cache.delete(key);
    this.cache.set(key, value); // 重新插入，标记为最近使用
    return value;
  }

  put(key, value) {
    if (this.cache.has(key)) this.cache.delete(key);
    else if (this.cache.size >= this.capacity) {
      this.cache.delete(this.cache.keys().next().value); // 删除最久未使用（第一个）
    }
    this.cache.set(key, value);
  }
}

const lru = new LRUCache(2);
lru.put(1, 'a'); lru.put(2, 'b');
lru.get(1);       // 访问 1，1 变为最近使用
lru.put(3, 'c');  // 容量满，淘汰最久未用的 2
console.log(lru.get(2)); // -1
```

**要点**：利用 `Map` 保持插入顺序的特性，`get`/`put` 时删除再重新插入即可实现"移到最近使用"，复杂度均为 O(1)。

---

## 11. 发布订阅模式（EventEmitter）

**场景**：组件通信（Vue2 的 `$emit`/`$on`）、模块解耦、自定义事件系统。

```javascript
class EventEmitter {
  constructor() {
    this.events = new Map();
  }

  on(event, callback) {
    if (!this.events.has(event)) this.events.set(event, []);
    this.events.get(event).push(callback);
    return this; // 支持链式调用
  }

  once(event, callback) {
    const wrapper = (...args) => {
      callback.apply(this, args);
      this.off(event, wrapper);
    };
    this.on(event, wrapper);
    return this;
  }

  off(event, callback) {
    if (!this.events.has(event)) return this;
    if (!callback) { this.events.delete(event); return this; }
    const filtered = this.events.get(event).filter((cb) => cb !== callback);
    this.events.set(event, filtered);
    return this;
  }

  emit(event, ...args) {
    if (!this.events.has(event)) return false;
    this.events.get(event).forEach((cb) => cb.apply(this, args));
    return true;
  }
}

const bus = new EventEmitter();
bus.on('login', (user) => console.log(`欢迎 ${user}`));
bus.emit('login', '小明'); // 欢迎 小明
```

**要点**：本质是用 `Map` 维护"事件名 → 回调数组"的映射；`once` 通过包一层自动 `off` 实现单次触发。

---

## 12. 手写 call / apply / bind

**场景**：理解 `this` 绑定机制，是原型链 & 执行上下文的经典考点。

```javascript
Function.prototype.myCall = function (context, ...args) {
  context = context || globalThis;
  const fnKey = Symbol('fn'); // 用 Symbol 避免覆盖已有属性
  context[fnKey] = this;      // this 指向调用 myCall 的函数
  const result = context[fnKey](...args);
  delete context[fnKey];
  return result;
};

Function.prototype.myApply = function (context, argsArray = []) {
  context = context || globalThis;
  const fnKey = Symbol('fn');
  context[fnKey] = this;
  const result = context[fnKey](...argsArray);
  delete context[fnKey];
  return result;
};

Function.prototype.myBind = function (context, ...args) {
  const fn = this;
  return function boundFn(...args2) {
    // new 调用时 this 指向新对象，此时不应绑定 context
    const isNew = this instanceof boundFn;
    return fn.apply(isNew ? this : context, args.concat(args2));
  };
};

function greet(greeting) { console.log(`${greeting}, ${this.name}`); }
greet.myCall({ name: '小红' }, '你好');   // 你好, 小红
greet.myApply({ name: '小刚' }, ['嗨']);  // 嗨, 小刚
const bound = greet.myBind({ name: '小李' });
bound('早上好'); // 早上好, 小李
```

**要点**：`call`/`apply` 本质是"让函数作为对象的属性调用"来改变 `this` 指向；`bind` 需额外处理 `new` 调用场景（构造函数场景下应忽略绑定的 `context`）。

---

## 复杂度速查表

| 算法 | 时间复杂度 | 空间复杂度 |
|---|---|---|
| 防抖/节流 | O(1) | O(1) |
| 深拷贝 | O(n) | O(n) |
| 数组扁平化 | O(n) | O(n) |
| 数组去重 | O(n) | O(n) |
| 柯里化 | O(1) 调用开销 | O(n) 闭包参数 |
| 二分查找 | O(log n) | O(1) |
| 快速排序 | 平均 O(n log n)，最差 O(n²) | O(log n) |
| LRU 缓存 get/put | O(1) | O(capacity) |
| 发布订阅 emit | O(k)，k为监听数 | O(n) |

---

以上均为可直接运行、无第三方依赖的原生 JS 实现，适合面试背诵、日常工具函数复用、以及理解框架底层原理（如 Vue 的响应式、EventBus、Promise 实现等）。
