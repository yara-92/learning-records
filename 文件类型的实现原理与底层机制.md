手工拼了一个最小的 `.docx`（见上面命令），`file` 命令直接把它识别成了 "Microsoft Word 2007+"——这就是接下来要讲的东西的最好证据：**docx 本质上就是一个改了后缀名的 zip 包**，Word 能打开它靠的不是什么魔法，就是按约定去解压、找 XML、渲染。

按这个思路把常见文件类型分几类讲。

## 1. 最底层：一切文件都是字节序列

操作系统层面没有"文件类型"这个东西，只有一串 bytes。"类型"是**应用层的约定**——靠后缀名提示，或者靠文件开头的"魔数"(magic number)标识。比如：

- PNG 开头固定是 `89 50 4E 47 0D 0A 1A 0A`
- ZIP（包括 docx/xlsx/pptx/jar/apk）开头是 `50 4B 03 04`（"PK"，致敬发明者 Phil Katz）
- ELF 可执行文件开头是 `7F 45 4C 46`

`file` 命令能秒判断文件类型，靠的就是读开头几个字节比对特征库，跟后缀名没关系（你把 demo.docx 改名成 xxx.txt，`file` 照样能认出它是 Word 文档）。

## 2. 纯文本类：txt / csv / md / json...

这类最简单，就是字符编码的约定问题：

- **字符编码**：ASCII（单字节）→ GBK/GB2312（中文常用双字节）→ UTF-8（变长，1~4字节，现在的事实标准）。同一份"你好"文本，UTF-8 编码是 6 字节，GBK 编码是 4 字节，编码方式不对就是乱码。
- **BOM**（Byte Order Mark）：文件开头的 `EF BB BF` 用来标识"这是 UTF-8"，非必须但 Windows 记事本爱加。
- **换行符**：Windows 用 `\r\n`（CRLF），Linux/macOS 用 `\n`（LF）——这就是为什么跨平台协作代码 diff 有时会莫名其妙全文件标红,git 里配 `.gitattributes` 就是管这个的。

## 3. 现代 Office 格式：docx / xlsx / pptx = ZIP + XML

这就是上面演示的东西。2007 年后微软把格式改成了 **OOXML**（Office Open XML，ISO 标准），内部结构：

```
demo.docx
├── [Content_Types].xml   ← 声明包里有哪些类型的内容
├── _rels/.rels           ← 关系表：根节点指向哪个部分是主文档
└── word/
    └── document.xml      ← 真正的正文内容，全是 XML 标签
```

xlsx 和 pptx 结构类似，只是内部目录名不同（`xl/worksheets/sheet1.xml`、`ppt/slides/slide1.xml`），逻辑是一样的：**一个 zip 容器，里面塞了一堆按规范组织的 XML 文件，靠 `.rels` 关系文件把它们串起来**。

这也解释了几个你可能遇到过的现象：
- xlsx 打不开时试试改后缀成 zip 再解压，经常能救回数据
- Word/Excel 文件损坏但能"部分恢复"，是因为不同 sheet/内容在不同 XML 文件里，坏一个不影响其他
- 你在 Vue+Supabase 项目里如果要做"导出报表"功能，直接手写这套 XML 结构、打包成 zip 就是一个可行方案（比引入完整 xlsx 库轻量，比如只要几个 sheet 数据）

## 4. 老式二进制格式：doc / xls / ppt（97-2003）

这批是 2007 年之前的格式，**不是 XML，是纯二进制**，用的是微软的 **OLE 复合文档格式**（Compound File Binary Format，CFBF）。可以理解成"文件系统嵌套在文件系统里"——一个 .doc 文件内部有自己的"目录结构"（叫 Storage）和"文件流"（叫 Stream），有点像一个迷你 FAT 文件系统打包在一个文件里。这也是为什么旧格式解析库（如 Apache POI 的 HSSF 部分）代码量巨大又晦涩——你得手撸一个简化版文件系统解析器,而不是喂给一个 XML parser 就完事。

## 5. 源代码文件：.py / .js —— 文本文件，但"被执行"的方式不同

这类本质上和 txt 没有区别（都是纯文本，UTF-8 编码），**区别在于"谁来读它、怎么处理它"**：

**.py（Python，解释执行为主）：**
1. 词法分析（Lexer）→ 把源码切成 token（`if`、`x`、`=`、`1` ...）
2. 语法分析（Parser）→ 组装成抽象语法树 AST
3. 编译成字节码（bytecode）→ 生成 `.pyc` 文件（CPython 特有，可以在 `__pycache__` 目录看到）
4. Python 虚拟机（CPython VM）逐条解释执行字节码

**.js（JavaScript，V8 引擎为例）：**
1. 同样经过 Lexer → Parser → AST
2. V8 有个 Ignition 解释器先生成字节码快速跑起来
3. 热点代码（反复执行的部分）会被 TurboFan JIT 编译器实时编译成机器码，这就是"即时编译"(JIT)——脚本语言想要接近原生速度靠的就是这招

一句话总结：**py/js 文件是文本，但打开方式不是"渲染显示"，而是"经过编译器/解释器的多级翻译流水线，最终变成 CPU 能执行的指令"**。这条流水线和 Word 打开 docx 走的"解压 zip → 解析 XML → 排版渲染"路径,在思路上其实是同构的：都是**按约定把结构化数据转成程序能理解的内存表示**。

## 6. 顺带一提的几类

- **PDF**：不是 zip，是自己的一套结构——对象（object）+ 交叉引用表（xref table，记录每个对象在文件里的字节偏移）+ 页面树。可以直接用编辑器打开一个简单 PDF 看到里面明文的 `1 0 obj ... endobj` 结构。
- **PNG/JPG**：PNG 是无损压缩(DEFLATE 算法，和 zip 用的算法系出同源)+分块结构(chunk)；JPG 是有损压缩,基于离散余弦变换(DCT)去掉人眼不敏感的高频信息。
- **压缩包 zip/tar.gz**：zip 是"容器+压缩"一体化(每个文件独立压缩,可单独提取,这也是 docx 选它做容器的原因之一);tar 只负责"打包"不压缩,压缩交给 gzip 叠加(所以叫 tar.gz,俗称"薄纸包裹")。
- **可执行文件 exe(Windows,PE 格式)/ELF(Linux)**：结构上也是"头部信息 + 多个 section(代码段.text、数据段.data等)",操作系统加载器读头部信息决定怎么把各段映射进内存、从哪条指令开始执行。

