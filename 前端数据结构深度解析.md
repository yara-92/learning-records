# 前端数据结构深度解析：原理、实现与实战应用

数据结构不是"面试背诵题"，而是前端框架和浏览器底层真实在用的东西。本文按"是什么 → JS 怎么实现 → 前端哪里真的在用"的顺序，讲透 9 种核心数据结构。

---

## 1. 栈（Stack）—— 后进先出 LIFO

### 原理
只能在一端（栈顶）进行插入（push）和删除（pop）的线性结构，"后进的先出去"，像一摞盘子。

```javascript
class Stack {
  #items = [];
  push(item) { this.#items.push(item); }
  pop() { return this.#items.pop(); }
  peek() { return this.#items[this.#items.length - 1]; }
  isEmpty() { return this.#items.length === 0; }
  get size() { return this.#items.length; }
}
```

### 前端应用

**① JS 引擎的函数调用栈（Call Stack）**
JS 是单线程语言，每次函数调用都会压入一个"执行上下文"到调用栈，函数执行完再弹出。递归过深导致 `Maximum call stack size exceeded` 就是栈溢出。这也是理解事件循环（调用栈清空后才执行微任务/宏任务）的基础。

**② 浏览器前进/后退历史**
`history.back()` / `history.forward()` 本质是两个栈：后退栈存已访问页面，前进栈存"后退过的页面"。点击新链接时前进栈会被清空——这正是"后退后再前进一个新地址，原来的前进记录会消失"的原因。

**③ 撤销/重做功能（Undo/Redo）**
在线文档、Canvas 画图工具（如语雀、Figma）常用两个栈实现：

```javascript
class HistoryManager {
  #undoStack = [];
  #redoStack = [];

  execute(state) {
    this.#undoStack.push(state);
    this.#redoStack = []; // 新操作后，之前的重做记录失效
  }
  undo() {
    if (this.#undoStack.length <= 1) return null;
    this.#redoStack.push(this.#undoStack.pop());
    return this.#undoStack[this.#undoStack.length - 1];
  }
  redo() {
    if (!this.#redoStack.length) return null;
    const state = this.#redoStack.pop();
    this.#undoStack.push(state);
    return state;
  }
}
```

**④ 括号/标签匹配校验**：JSX 语法解析、CSS 选择器嵌套校验、模板字符串括号匹配，都用栈来配对。

---

## 2. 队列（Queue）—— 先进先出 FIFO

### 原理
只能从一端插入（rear，队尾）、另一端删除（front，队头），"先来的先服务"。

```javascript
class Queue {
  #items = [];
  enqueue(item) { this.#items.push(item); }
  dequeue() { return this.#items.shift(); } // O(n)，生产环境建议用双指针数组或链表优化
  get front() { return this.#items[0]; }
  get isEmpty() { return this.#items.length === 0; }
}
```

### 前端应用

**① 事件循环中的任务队列（Task Queue）**
JS 的宏任务队列（`setTimeout`、`I/O`）和微任务队列（`Promise.then`、`MutationObserver`）本质都是队列，调用栈清空后依次从队列中取任务执行——这是整个 JS 异步机制的核心数据结构。

**② React 的更新调度队列**
React 内部用队列缓存多次 `setState` 的更新对象（Update Queue），批量处理后一次性重新渲染，避免每次 `setState` 都触发一次渲染。

**③ 请求限流/并发控制队列**
前端常见的"最多同时 3 个请求，超出的排队"场景：

```javascript
class RequestQueue {
  #queue = [];
  #running = 0;
  constructor(maxConcurrent = 3) { this.max = maxConcurrent; }

  add(task) {
    return new Promise((resolve, reject) => {
      this.#queue.push({ task, resolve, reject });
      this.#run();
    });
  }

  #run() {
    while (this.#running < this.max && this.#queue.length) {
      const { task, resolve, reject } = this.#queue.shift();
      this.#running++;
      task().then(resolve, reject).finally(() => {
        this.#running--;
        this.#run();
      });
    }
  }
}
// 用法：图片懒加载批量请求、上传大文件分片并发控制
```

**④ 消息提示队列**：Ant Design/Element 的 `message.success()` 连续调用时，多条消息依次排队展示，避免同时挤在一起。

---

## 3. 链表（Linked List）—— 用指针串联的动态结构

### 原理
每个节点存数据和"指向下一个节点的指针"，插入/删除只需改指针（O(1)），但查找必须从头遍历（O(n)），与数组（查找快、插入删除慢）正好互补。

```javascript
class ListNode {
  constructor(val) { this.val = val; this.next = null; }
}
class LinkedList {
  constructor() { this.head = null; this.size = 0; }
  append(val) {
    const node = new ListNode(val);
    if (!this.head) { this.head = node; }
    else {
      let cur = this.head;
      while (cur.next) cur = cur.next;
      cur.next = node;
    }
    this.size++;
  }
}
```

### 前端应用

**① React Fiber 架构（核心！）**
React 16 之后的 Fiber 节点本质就是一个链表结构：每个 Fiber 有 `child`（第一个子节点）、`sibling`（下一个兄弟节点）、`return`（父节点）三个指针。这种设计让 React 可以中断渲染、保存现场、之后再从断点继续遍历（可中断的可恢复的遍历），是实现 **时间切片（Time Slicing）** 和并发模式的基础，比传统递归遍历虚拟 DOM 树更灵活。

```javascript
// Fiber 节点简化结构
{
  type: 'div',
  child: FiberNode,    // 指向第一个子节点
  sibling: FiberNode,  // 指向下一个兄弟节点
  return: FiberNode,   // 指向父节点
}
```

**② LRU 缓存**：上一份文档中用 `Map` 实现的 LRU，其经典实现（不借助 Map）就是"双向链表 + 哈希表"——链表维护访问顺序，哈希表实现 O(1) 查找节点位置。

**③ 浏览器的 DOM 树同层节点**：`nextSibling` / `previousSibling` / `firstChild` 就是典型的链表指针操作。

---

## 4. 哈希表（Hash Table / Map）—— 用空间换时间的查找利器

### 原理
通过哈希函数把 key 映射到存储位置，实现平均 O(1) 的增删查。JS 中 `Object` 和 `Map` 都是哈希表的具体实现（`Map` 更纯粹，`Object` 的 key 会被强制转字符串且有原型链干扰）。

### 前端应用

**① Vue 响应式系统的依赖收集（核心！）**
Vue 3 的响应式核心 `targetMap` 就是一个多层嵌套的 `WeakMap`：

```javascript
// Vue3 响应式简化原理
const targetMap = new WeakMap(); // target -> Map
// target -> (key -> Set<effect>)

function track(target, key) {
  let depsMap = targetMap.get(target);
  if (!depsMap) targetMap.set(target, (depsMap = new Map()));
  let dep = depsMap.get(key);
  if (!dep) depsMap.set(key, (dep = new Set()));
  dep.add(activeEffect); // 收集当前正在执行的副作用函数
}
```
`WeakMap` 用对象本身做 key（不会造成内存泄漏，对象被回收时依赖自动清除），`Map` 存该对象每个属性对应的依赖集合，`Set` 存依赖该属性的所有副作用函数——三层哈希结构撑起了整个响应式系统。

**② React Hooks 的状态存储**：每个函数组件对应一个 Fiber，Hooks 状态以链表形式挂在 Fiber 上，但 `useContext`、`useMemo` 缓存判断等场景大量依赖 `Map`/`WeakMap` 做依赖对比。

**③ 前端路由表**：`Vue Router` / `React Router` 内部用哈希表存 `path -> component` 的映射，实现 O(1) 路由匹配（动态路由/嵌套路由除外，需配合 Trie 或正则）。

**④ 组件属性 diff、事件委托的 handler 映射、请求缓存（如 SWR/React Query 的 cache key）**：本质都是哈希表应用。

---

## 5. 树（Tree）—— 前端最核心的数据结构

### 原理
一个节点可以有多个子节点，子节点之间无连接，是一种层级结构。

### 前端应用（几乎无处不在）

**① DOM 树**：HTML 文档本身就是树形结构，`document.body` 是根节点，浏览器渲染引擎基于 DOM 树 + CSSOM 树构建 Render Tree。

**② 虚拟 DOM（Virtual DOM）与 Diff 算法**
React/Vue 用 JS 对象模拟 DOM 树结构，更新时对比新旧两棵树（Diff），只更新变化的部分再打补丁到真实 DOM。传统树的最小编辑距离算法是 O(n³)，React 通过三个策略把复杂度降到 O(n)：

```javascript
// Diff 三大简化策略
// 1. 同层比较：只比较同一层级节点，不跨层级移动比较
// 2. Type 不同直接替换整棵子树，不再深入比较
// 3. 列表用 key 标识，通过 key 判断节点是复用/新增/删除
function diffChildren(oldChildren, newChildren) {
  const oldKeyMap = new Map(oldChildren.map((c) => [c.key, c]));
  return newChildren.map((newChild) => {
    const oldChild = oldKeyMap.get(newChild.key);
    return oldChild ? patch(oldChild, newChild) : mount(newChild); // 复用 or 新建
  });
}
```

**③ 组件树**：React/Vue 组件本身是嵌套的树形结构，Context 的层层传递、事件冒泡/捕获，都基于树的父子关系。

**④ 前端菜单/评论/文件目录**：上一份文档讲的"扁平数组转树"就是把接口数据还原成树，用于渲染多级菜单、评论回复、文件夹结构。

**⑤ 二叉搜索树（BST）的进阶应用**：虽然前端业务代码较少直接手写 BST，但理解其"左小右大"的特性有助于理解数据库索引、以及某些排序可视化算法题。

```javascript
class BSTNode {
  constructor(val) { this.val = val; this.left = null; this.right = null; }
}
class BST {
  root = null;
  insert(val) {
    const newNode = new BSTNode(val);
    if (!this.root) { this.root = newNode; return; }
    let cur = this.root;
    while (true) {
      if (val < cur.val) {
        if (!cur.left) { cur.left = newNode; return; }
        cur = cur.left;
      } else {
        if (!cur.right) { cur.right = newNode; return; }
        cur = cur.right;
      }
    }
  }
  // 中序遍历得到有序序列
  inorder(node = this.root, res = []) {
    if (node) {
      this.inorder(node.left, res);
      res.push(node.val);
      this.inorder(node.right, res);
    }
    return res;
  }
}
```

---

## 6. 堆（Heap）—— 优先级调度的核心

### 原理
一种特殊的完全二叉树：大顶堆每个父节点都大于等于子节点，小顶堆反之。用数组模拟即可（下标 `i` 的父节点是 `(i-1)>>1`，子节点是 `2i+1` 和 `2i+2`），插入/删除堆顶复杂度都是 O(log n)。

```javascript
class MinHeap {
  #heap = [];
  #parent(i) { return (i - 1) >> 1; }

  push(val) {
    this.#heap.push(val);
    let i = this.#heap.length - 1;
    while (i > 0 && this.#heap[this.#parent(i)] > this.#heap[i]) {
      [this.#heap[i], this.#heap[this.#parent(i)]] = [this.#heap[this.#parent(i)], this.#heap[i]];
      i = this.#parent(i);
    }
  }

  pop() {
    const top = this.#heap[0];
    const last = this.#heap.pop();
    if (this.#heap.length) {
      this.#heap[0] = last;
      this.#sinkDown(0);
    }
    return top;
  }

  #sinkDown(i) {
    const n = this.#heap.length;
    while (true) {
      let smallest = i, left = 2 * i + 1, right = 2 * i + 2;
      if (left < n && this.#heap[left] < this.#heap[smallest]) smallest = left;
      if (right < n && this.#heap[right] < this.#heap[smallest]) smallest = right;
      if (smallest === i) break;
      [this.#heap[i], this.#heap[smallest]] = [this.#heap[smallest], this.#heap[i]];
      i = smallest;
    }
  }
}
```

### 前端应用

**① React Scheduler 任务调度（核心！）**
React 18 的并发渲染依赖一个内部调度器（Scheduler），用**小顶堆**管理任务优先级：不同优先级的更新（如用户输入 vs 后台数据请求）会被赋予不同的过期时间戳，调度器每次从堆顶取"最紧急（过期时间最早）"的任务执行，配合 `MessageChannel` 实现时间切片，避免长任务阻塞主线程导致页面卡顿。

**② TOP K 问题**：前端展示"热搜榜前 10"、"最大的 5 个文件"等场景，用堆维护固定大小的候选集，比全量排序（O(n log n)）更高效（O(n log k)）。

```javascript
function topK(nums, k) {
  const heap = new MinHeap();
  for (const num of nums) {
    heap.push(num);
    if (heap.size > k) heap.pop(); // 堆大小超过k就把最小的踢出去
  }
  return heap; // 剩下的就是最大的k个
}
```

---

## 7. 图（Graph）—— 描述复杂关系网络

### 原理
由"节点（顶点）"和"边"组成，边可以有方向、有权重。常用**邻接表**（哈希表存储每个节点的邻居）表示：

```javascript
class Graph {
  #adjList = new Map();
  addVertex(v) { if (!this.#adjList.has(v)) this.#adjList.set(v, []); }
  addEdge(v1, v2) {
    this.addVertex(v1); this.addVertex(v2);
    this.#adjList.get(v1).push(v2);
    this.#adjList.get(v2).push(v1); // 无向图双向添加
  }
  bfs(start) {
    const visited = new Set([start]);
    const queue = [start];
    const order = [];
    while (queue.length) {
      const v = queue.shift();
      order.push(v);
      for (const neighbor of this.#adjList.get(v)) {
        if (!visited.has(neighbor)) { visited.add(neighbor); queue.push(neighbor); }
      }
    }
    return order;
  }
}
```

### 前端应用

**① Webpack/Vite 的模块依赖图（核心！）**
构建工具本质上是在构建一张巨大的**有向图**：每个模块是一个节点，`import` 语句是边。打包时对这张图做 **拓扑排序**（保证被依赖的模块先打包）+ **DFS 遍历**（收集所有依赖），并通过检测"环"来发现循环依赖问题。

**② npm 包依赖树/依赖图**：`package.json` 的依赖关系、`peerDependencies` 冲突检测，本质是图的遍历与环检测问题。

**③ 状态管理的依赖追踪**：Vue 响应式系统中一个 computed 依赖多个响应式数据、多个 computed 之间又互相依赖，这种多对多关系本质是有向无环图（DAG），需要正确的拓扑顺序来避免重复计算或死循环。

**④ 社交网络/关系图谱可视化**：如 ECharts/AntV G6 画的组织架构图、知识图谱，数据模型就是图，力导向布局算法（Force-Directed）也是图算法的应用。

---

## 8. 字典树（Trie / 前缀树）—— 字符串前缀匹配神器

### 原理
每个节点代表一个字符，从根到某节点的路径拼接起来就是一个字符串前缀，适合"按前缀查找"场景，查找时间复杂度只与字符串长度相关（O(m)），与数据总量无关。

```javascript
class TrieNode {
  children = new Map();
  isEnd = false;
}
class Trie {
  root = new TrieNode();

  insert(word) {
    let node = this.root;
    for (const char of word) {
      if (!node.children.has(char)) node.children.set(char, new TrieNode());
      node = node.children.get(char);
    }
    node.isEnd = true;
  }

  // 查找所有以 prefix 开头的单词（自动补全核心逻辑）
  findByPrefix(prefix) {
    let node = this.root;
    for (const char of prefix) {
      if (!node.children.has(char)) return [];
      node = node.children.get(char);
    }
    const results = [];
    const dfs = (node, path) => {
      if (node.isEnd) results.push(path);
      node.children.forEach((child, char) => dfs(child, path + char));
    };
    dfs(node, prefix);
    return results;
  }
}

const trie = new Trie();
['react', 'redux', 'router', 'reactive'].forEach((w) => trie.insert(w));
console.log(trie.findByPrefix('re')); // ['react', 'redux', 'router', 'reactive']
```

### 前端应用

**① 搜索框自动补全/联想词（核心！）**：如电商搜索框、IDE 的代码自动提示（VS Code 的 IntelliSense），本质都是 Trie 树的前缀查找，比用数组遍历 `filter(str => str.startsWith(prefix))` 在数据量大时效率高得多。

**② 前端路由动态匹配**：某些高性能路由库（如早期 Express 风格路由）用 Trie 组织路径片段（`/user/:id/profile`），实现比正则更快的路径匹配。

**③ 敏感词过滤**：评论/弹幕系统的敏感词库常构建成 Trie（或 AC 自动机，Trie 的进阶），一次遍历文本即可同时匹配所有敏感词。

---

## 数据结构 × 前端应用速查表

| 数据结构 | 时间复杂度（查找/插入） | 前端核心应用场景 |
|---|---|---|
| 栈 | O(n) / O(1) | 调用栈、浏览器历史、撤销重做 |
| 队列 | O(n) / O(1) | 事件循环任务队列、请求并发控制 |
| 链表 | O(n) / O(1) | **React Fiber**、LRU 缓存 |
| 哈希表 | O(1) / O(1) | **Vue 响应式依赖收集**、路由表、缓存 |
| 树 | O(log n)~O(n) | **DOM 树**、**虚拟 DOM Diff**、组件树 |
| 堆 | O(log n) | **React Scheduler 优先级调度**、TOP K |
| 图 | O(V+E) 遍历 | **Webpack 依赖图**、状态依赖追踪、关系可视化 |
| Trie | O(m)，m为字符串长度 | 搜索自动补全、敏感词过滤、路由匹配 |

---

## 一句话总结

前端框架的性能优化几乎都能追溯到数据结构的选择：**React 用链表结构（Fiber）实现可中断渲染，用堆实现优先级调度；Vue 用多层哈希表实现精确依赖追踪；构建工具用图实现依赖分析；搜索类功能靠 Trie 实现高效前缀匹配**。理解这些底层结构，不仅能应付面试，更能在阅读源码、排查性能问题时真正做到"知其所以然"。
