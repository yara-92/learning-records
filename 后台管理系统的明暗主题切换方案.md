实现明暗主题切换,核心思路是**用 CSS 变量做主题令牌层,配合一个顶层的模式开关(class 或 data 属性),再解决持久化和无闪烁问题**。展开说说各个环节。

## 1. 技术方案选型

### 方案 A:CSS 自定义属性(推荐,最主流)

在根节点(`:root` 或 `html`)上定义一套语义化变量,不同主题下切换变量值:

```css
:root {
  --bg-page: #f5f7fa;
  --bg-container: #ffffff;
  --bg-sidebar: #ffffff;
  --text-primary: #1f2329;
  --text-secondary: #646a73;
  --border-color: #e5e6eb;
  --shadow-color: rgba(0, 0, 0, 0.08);
}

html[data-theme='dark'] {
  --bg-page: #17181a;
  --bg-container: #1f2022;
  --bg-sidebar: #1f2022;
  --text-primary: #e5e6eb;
  --text-secondary: #a8abb2;
  --border-color: #333438;
  --shadow-color: rgba(0, 0, 0, 0.3);
}
```

业务组件里只用变量,不写死颜色:

```css
.sidebar {
  background: var(--bg-sidebar);
  border-right: 1px solid var(--border-color);
}
```

**优点**:切换只是改一个属性,浏览器原生支持,性能好(不需要重新加载样式表),且可以配合 `transition` 做平滑过渡动画。这是目前 Element Plus、Ant Design Vue、Naive UI 等库都在用的方案。

### 方案 B:两套完整样式表切换

准备 `theme-light.css` / `theme-dark.css`,切换时替换 `<link>` 标签。缺点是切换会有短暂白屏/闪烁,而且两套样式难以维护同步,现在基本没人用了,了解即可。

### 方案 C:Tailwind 的 `dark:` 变体

如果你用 Tailwind,直接用 `darkMode: 'class'` 配置,组件里写 `bg-white dark:bg-gray-900`。本质上和方案 A 是同一套逻辑(也是靠 `html.dark` 这个 class 驱动),只是 Tailwind 帮你生成了变体类。

**实践中我会建议方案 A 为主(定义语义变量),即使用 Tailwind 也可以让 Tailwind 的 dark 类背后指向这些变量**,这样将来要加"跟随系统"或者第三套主题(比如护眼色)时,扩展性更好。

## 2. 你项目里具体怎么落地(Vue 3 + Element Plus 场景)

既然你在用 Element Plus 之类组件库,要注意**组件库自己也有一套暗色变量**,不是纯自己从零写。以 Element Plus 为例:

- 引入官方暗黑模式样式:`element-plus/theme-chalk/dark/css-vars.css`
- 通过给 `html` 加 `class="dark"` 来触发它的暗色变量(Element Plus 底层也是 CSS 变量方案,和 Tailwind 的 dark class 天然可以共用一个开关)
- 你自己业务层的变量(上面方案 A 那套)独立维护,和组件库变量并行,不要试图去覆盖组件库内部变量,容易踩坑

一个 Vue 3 的主题 composable 大致这样:

```ts
// useTheme.ts
import { ref, watchEffect } from 'vue'

type ThemeMode = 'light' | 'dark' | 'auto'

const mode = ref<ThemeMode>((localStorage.getItem('theme-mode') as ThemeMode) || 'auto')

const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)')

function applyTheme() {
  const isDark = mode.value === 'dark' || (mode.value === 'auto' && mediaQuery.matches)
  document.documentElement.classList.toggle('dark', isDark)
  document.documentElement.setAttribute('data-theme', isDark ? 'dark' : 'light')
}

export function useTheme() {
  watchEffect(() => {
    applyTheme()
    localStorage.setItem('theme-mode', mode.value)
  })

  mediaQuery.addEventListener('change', () => {
    if (mode.value === 'auto') applyTheme()
  })

  return { mode }
}
```

要点:
- 同时设置 `class="dark"`(给 Tailwind/Element Plus 用)和 `data-theme="dark"`(给你自己的变量体系用),两条腿走路,后续换库也方便
- 支持三态:`light` / `dark` / `auto`(跟随系统),`auto` 是很多产品现在的默认值

## 3. 关键坑点

**闪烁问题(FOUC)**:如果主题状态存在 localStorage,但 Vue 应用要等 JS 加载执行后才应用 class,页面会先按默认主题渲染,再瞬间切换,出现闪屏。解决办法是在 `index.html` 的 `<head>` 里、Vue 挂载之前,用一段内联同步脚本提前读取并设置好:

```html
<script>
  (function() {
    const mode = localStorage.getItem('theme-mode') || 'auto'
    const isDark = mode === 'dark' || (mode === 'auto' && window.matchMedia('(prefers-color-scheme: dark)').matches)
    document.documentElement.classList.toggle('dark', isDark)
  })()
</script>
```

**第三方组件/富文本内容**:像 ECharts 图表、富文本编辑器内容、iframe 嵌入内容,这些往往有自己的颜色系统,不会自动响应你的 CSS 变量,需要单独监听主题变化手动传入配色(比如 ECharts 切换 theme 实例)。

**图片/图标反色**:纯色 icon 建议用 SVG + `currentColor` 让它自动跟随文字色变化;截图类图片在暗色下会显得刺眼,可以考虑 `filter: brightness(0.8) contrast(1.2)` 做简单柔化,或者干脆准备双图。

**过渡动画**:想要切换时有平滑过渡而不是突兀跳变,可以给根节点及关键容器加:

```css
* {
  transition: background-color 0.3s, border-color 0.3s, color 0.3s;
}
```

但注意别对所有元素全局加,可能会拖慢一些复杂动画组件,建议限定在容器级别。

## 4. 状态存储与跨端同步

- 本地持久化:localStorage 足够,如果有多标签页场景可以用 `storage` 事件同步
- 如果是登录用户,可以把主题偏好存到后端用户设置里,跨设备同步(结合你 ERP/OA 系统的用户表加一个字段就行)

---
