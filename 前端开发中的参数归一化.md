# 前端开发中的参数归一化（Parameter Normalization）

参数归一化其实不是一个单一概念，在前端里通常有好几种不同的含义，取决于语境。我按常见程度展开讲。

## 1. 函数参数归一化（最常见的用法）

这是指把一个函数**可能接收的多种输入形式**，统一转换成内部处理时使用的**标准格式**。目的是让 API 对使用者友好（允许多种调用方式），同时让内部实现逻辑简单（只处理一种数据形状）。

典型场景：一个函数既想支持简写调用，又想支持完整配置对象。

```javascript
// 用户可能这样调用：
fetchData('url')
fetchData('url', { method: 'POST' })
fetchData({ url: 'url', method: 'GET', headers: {...} })

// 归一化处理
function normalizeOptions(urlOrOptions, options = {}) {
  if (typeof urlOrOptions === 'string') {
    return { url: urlOrOptions, ...options };
  }
  return urlOrOptions;
}

function fetchData(urlOrOptions, options) {
  const config = normalizeOptions(urlOrOptions, options);
  // 后续逻辑只需要处理 config 这一种形状
}
```

这种模式在很多知名库里都能看到，比如 jQuery 的 `$.ajax`、Vue Router 的路由配置、Element Plus 等组件库的 props 处理（比如允许 `size` 既接受字符串又接受对象）。

**好处**：调用方灵活，内部逻辑单一，减少大量 `if/else` 分支散落在业务代码各处。

## 2. 数据结构归一化（Normalization of Nested Data）

这个概念主要出现在**状态管理**场景，尤其是 Redux 生态（`normalizr` 库是代表）。指把嵌套的、有冗余的 API 响应数据，转换成**扁平的、以 ID 为索引的结构**，类似数据库的范式化设计。

比如接口返回：

```javascript
{
  id: 1,
  title: '文章A',
  author: { id: 5, name: '张三' },
  comments: [
    { id: 10, text: '不错', author: { id: 5, name: '张三' } }
  ]
}
```

归一化后：

```javascript
{
  posts: { 1: { id: 1, title: '文章A', author: 5, comments: [10] } },
  comments: { 10: { id: 10, text: '不错', author: 5 } },
  users: { 5: { id: 5, name: '张三' } }
}
```

**为什么这么做**：
- 同一个实体（比如“张三”这个用户）在多处出现时，只存一份，避免数据不一致
- 更新某个实体时，只需改一处，配合 selector 自动反映到所有引用它的地方
- 更适合用 O(1) 的方式按 ID 查找，而不是遍历嵌套数组

## 3. 数值归一化（Min-Max / 区间映射）

这个更偏数学，常出现在**数据可视化、Canvas 动画、图表库**里，把任意范围的数值映射到统一区间（通常是 0~1，或者像素坐标）。

```javascript
// 把 value 从 [min, max] 映射到 [0, 1]
function normalize(value, min, max) {
  return (value - min) / (max - min);
}

// 常见用途：把数据映射到画布像素坐标
function scaleToCanvas(value, dataMin, dataMax, canvasWidth) {
  const t = normalize(value, dataMin, dataMax);
  return t * canvasWidth;
}
```

D3.js 的 `d3.scaleLinear()`，Chart.js 内部计算轴刻度时，本质上都在做这类归一化。

## 4. 请求 / 缓存 Key 参数归一化

在做**请求去重、缓存（如 React Query / SWR）**时，同样的参数如果顺序或写法不同，会被当成不同的 key，导致缓存失效或重复请求。这时需要归一化参数：

```javascript
// { page: 1, sort: 'asc' } 和 { sort: 'asc', page: 1 } 应该视为同一个 key
function normalizeParams(params) {
  return Object.keys(params)
    .sort()
    .reduce((acc, key) => {
      if (params[key] !== undefined && params[key] !== null) {
        acc[key] = params[key];
      }
      return acc;
    }, {});
}

const cacheKey = JSON.stringify(normalizeParams(rawParams));
```

React Query 内部对 queryKey 的序列化机制、Axios 的 paramsSerializer，本质上都在解决这个问题。

## 实践建议

- **函数参数归一化**：适合在函数入口处集中处理，写一个 `normalize` 辅助函数，避免归一化逻辑散落在主逻辑各处
- **数据归一化**：不是所有场景都需要，简单页面直接用嵌套数据即可；只有当同一实体在多处被引用、且需要频繁更新时，归一化才划算，否则会增加代码复杂度
- **数值归一化**：注意除零保护（`max === min` 时）
- **参数/缓存归一化**：注意深层嵌套对象和数组顺序的处理，简单的 key 排序对嵌套对象不够，可能需要递归归一化

---
